# Mini Message Board

## Features:
1. Create and read messages
2. Each message has a unique ID so you can open and read the details of a message!

## Steps:
1. Run `npm i`
2. Run `npm run dev` or `node app.js`
3. Open `localhost:3000` in your browser